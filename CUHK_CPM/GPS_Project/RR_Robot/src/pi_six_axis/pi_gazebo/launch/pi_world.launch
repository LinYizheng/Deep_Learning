<launch>
	<include file="$(find gazebo_ros)/launch/empty_world.launch">
		<arg name="world_name" value="$(find pi_gazebo)/worlds/pi.world"/>
		<arg name="gui" value="true"/>
	</include>
  <!-- <rosparam file="$(find pi_gazebo)/config/pi_control.yaml" command="load"/> -->

    <param name="robot_description" command="$(find xacro)/xacro.py '$(find pi_description)/robots/pi.urdf.xacro'"/>

	<node name="pi_description" pkg="gazebo_ros" type="spawn_model" output="screen" args="-urdf -param robot_description -model mybot" />


	  <!-- load the controllers -->
<!-- 	  <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false"
	  output="screen" ns="/pi" args="left_finger_controller right_finger_controller" /> -->
  	   <!-- <node name="gui" pkg="pi_gazebo" type="pi_six_axis_gui.py"/>   -->
</launch>
